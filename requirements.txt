fastapi
uvicorn[standard]
pydantic
lunar_python
jinja2
timezonefinder
tzdata

# Джйотиш: pyswisseph. На Render (Linux) ставится обычным образом — компилятор
# там уже есть в образе сборки. На Windows у пакета нет готового wheel под эту
# версию Python — локально ставится только после Microsoft C++ Build Tools.
pyswisseph
